# completion-gen

```
# build image
make build

# generate docker.ts
make run

# then copy dist/docker.ts file to autocomplete/src directory.
cp dist/docker.ts path_to_autocomplete/src

# check
cd path_to_autocomplete; npm run dev
```
